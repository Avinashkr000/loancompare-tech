# Frontend route architecture

Routes are currently kept in `src/App.jsx` to keep the MVP compact. The route groups are:

- Public product pages
- Authentication pages
- Protected dashboard pages
- Nested application detail pages
- Admin operations page
