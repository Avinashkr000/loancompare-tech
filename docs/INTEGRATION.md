# Integration boundaries

The domain is intentionally separated at lender comparison and application creation. Replace the seeded lender repository with authenticated partner clients; keep `LoanOffer` as the normalized internal contract for the frontend.
