# Full-stack delivery

Frontend is now a routed React product with authenticated and nested dashboard pages. Backend is Express + Prisma + PostgreSQL with JWT authentication, lender comparison, applications, profile and admin APIs. Docker Compose provides Postgres, API and Nginx frontend services. CI validates frontend build and backend schema/syntax.
